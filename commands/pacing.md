---
description: Compare usage % vs calendar time % to check quota pacing
argument-hint: <paste /usage output>
---

Feed the text below into `~/.claude/scripts/pacing.py` via stdin using input redirection with a heredoc (`python3 script.py <<'EOF'`), NOT a pipe. Display the output verbatim. No additional commentary.

$ARGUMENTS
